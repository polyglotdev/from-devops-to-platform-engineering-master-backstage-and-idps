apiVersion: v1
kind: Service
metadata:
  name: fastapi-app
  namespace: platform-demo
  labels:
    app.kubernetes.io/name: fastapi-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: fastapi-app
